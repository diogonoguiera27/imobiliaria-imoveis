import React from 'react';
import { LoginLayout, LoginForm } from '@/components/Login';

const LoginPage: React.FC = () => (
  <LoginLayout>
    <LoginForm />
  </LoginLayout>
);

export default LoginPage;
